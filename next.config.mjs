/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            'randomuser.me',
            'cdn.prod.website-files.com'
        ]
    }
};

export default nextConfig;
